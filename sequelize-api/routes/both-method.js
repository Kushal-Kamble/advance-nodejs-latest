/*

🎈 1) पहला तरीका (Destructuring वाला तरीका)

तुमने पहले ऐसा किया था:

const {
    getAllRestaurants,
    getRestaurantById,
    createRestaurant,
    updateRestaurant,
    deleteRestaurant
} = require("../controllers/restaurantController");

🧒 5 साल वाले बच्चे की भाषा में समझो:

मान लो तुम्हारा एक टिफ़िन बॉक्स है जिसमें 5 चीजें रखी हैं:
🍎 → getAllRestaurants
🍌 → getRestaurantById
🍇 → createRestaurant
🍉 → updateRestaurant
🍍 → deleteRestaurant

Destructuring मतलब —
टिफ़िन बॉक्स खोलकर सारी चीजें अलग-अलग हाथ में पकड़ लेना।

इसलिए तुम route ऐसे लिखते हो:

router.get("/", getAllRestaurants);
router.post("/", createRestaurant);


मतलब हर चीज़ अलग-अलग नाम से आ गई।

🎈 2) दूसरा तरीका (controller. वाला तरीका)

तुमने Example में ऐसा दिया:

const controller = require("../controllers/contactController");

router.post("/", controller.createContact);
router.get("/", controller.getContacts);
router.get("/:id", controller.getContact);
router.put("/:id", controller.updateContact);
router.delete("/:id", controller.deleteContact);

🧒 5 साल वाले बच्चे की भाषा में समझो:

यह ऐसा है जैसे तुम्हारे पास एक बड़ा बैग है जिसका नाम controller है।

उस बैग के अंदर 5 खिलौने रखे हैं:

createContact

getContacts

getContact

updateContact

deleteContact

और तुम हर बार कहते हो:

"controller वाला खिलौना निकाल दो।"

यानी तुम्हें हर बार controller. लगाना पड़ता है।

यह तरीका बिल्कुल valid है।


🎯 दोनों तरीकों का फर्क क्या है?
तरीका	नाम	कैसा लगता है?	फायदा	नुकसान
1) Destructuring	छोटा नाम	साफ-सुथरा	बार-बार controller. नहीं लिखना पड़ता	फाइल में कई नाम import होते हैं
2) controller. वाला	लंबा नाम	readable	साफ पता चलता है किस controller से आया	हर बार controller. लिखना पड़ता है




*/